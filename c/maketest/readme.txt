example program - makefile

base project using a makefile.

illustrates using separate source and object directories.

user need only define source files (with directories) and executable filename.

