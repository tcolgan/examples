command-line calculator

type "clc help" for details.

type "clc source" to generate source code.

command-line calculator is a program for doing basic calculations.

supports most basic "c" math functions (trig functions, logarithms, etc.)

all calculations done internally in floating point.

input/output can be of various decimal types.

supports binary, decimal, hexadecimal representations.

